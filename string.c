#include <stdio.h>
#include <math.h>

int main()
{
    // char name[100] = "asjal";
    // // fgets(name, 100, stdin);
    // puts(name);

    // // STRING LENGTH ***************
    // int length = strlen(name);
    // printf("%d", length);

    // // STRING COPY OLD TO NEW********
    // char hellow[100] = "hellow";
    // char world[100] = "world";
    // puts(hellow);
    // puts(world);

    // // strcpy(world, hellow);
    
    // // puts(hellow);
    // // puts(world); 

    // //STRING CONCATENATE
    // strcat(hellow, world);
    // puts(hellow);
    // puts(world);

    // //INPUT STRING USING %C***********
    // char naam[100];
    // char ch;
    // int i = 0;

    // while (ch != '\n')
    // {
    //     scanf("%c", &ch);
    //     naam[i] = ch;
    //     i++;
    // }
    
    // naam[i] = '\0';
    // puts(naam);

    char *str = "asjal amir";
    printf("%s", str);
    
    return 0;
}
